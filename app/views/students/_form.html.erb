<div class="n-wrapper">
 <div class="student-card">
    <div class="card-header-solid">
      <h2 class="mb-0"><%= student.new_record? ? "ADDING NEW STUDENT" : "EDITING STUDENT" %></h5>
    </div>

    <div class="card-body">
      <%= form_with(model: student, local: true) do |form| %>
        <div class="mb-3">
          <%= form.label :name, "Name" %>
          <%= form.text_field :name, class: "form-control", placeholder: "Enter student name" %>
        </div>

        <div class="mb-3">
          <%= form.label :year_level, "Year level" %>
          <%= form.number_field :year_level, class: "form-control", placeholder: "Enter year level", min: 1 %>
        </div>

        <div class="mb-3">
          <%= form.label :program, "Program" %>
          <%= form.text_field :program, class: "form-control", placeholder: "Enter program/course" %>
        </div>

        <div class="mb-3">
          <%= form.label :department_id, "Department" %>
          <%= form.collection_select :department_id, Department.all, :id, :name, { prompt: "Select a department" }, class: "form-select" %>
        </div>

        <div class="text-end mt-4">
          <%= form.submit student.new_record? ? "Create Student" : "Update Student", class: "btn-n btn-peach" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
